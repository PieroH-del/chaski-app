<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".ChaskiApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppChaski"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Splash Activity como punto de entrada -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppChaski.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity con Bottom Navigation -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false"
            android:label="@string/app_name" />

        <!-- Autenticación -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Productos y Carrito -->
        <activity
            android:name=".ui.productos.ProductosActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity" />

        <activity
            android:name=".ui.carrito.CarritoActivity"
            android:exported="false"
            android:parentActivityName=".ui.productos.ProductosActivity" />

        <activity
            android:name=".ui.checkout.CheckoutActivity"
            android:exported="false"
            android:parentActivityName=".ui.carrito.CarritoActivity" />

        <activity
            android:name=".ui.pedido.PedidoExitoActivity"
            android:exported="false" />

        <activity
            android:name=".ui.pedido.DetallePedidoActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity" />

        <!-- Perfil y Direcciones -->
        <activity
            android:name=".ui.perfil.EditarPerfilActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.direcciones.DireccionesActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity" />

        <activity
            android:name=".ui.direcciones.AgregarDireccionActivity"
            android:exported="false"
            android:parentActivityName=".ui.direcciones.DireccionesActivity"
            android:windowSoftInputMode="adjustResize" />

    </application>

</manifest>